# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf

##################
### Wallpapers ###
##################

$w1 = awww img ~/Pictures/Wallpapers/w_1.jpg -t none --filter Nearest
$w2 = awww img ~/Pictures/Wallpapers/w_2.jpg -t none --filter Nearest
$w3 = awww img ~/Pictures/Wallpapers/w_3.jpg -t none --filter Nearest
$w4 = awww img ~/Pictures/Wallpapers/w_4.jpg -t none --filter Nearest
$w5 = awww img ~/Pictures/Wallpapers/w_5.jpg -t none --filter Nearest
$w6 = awww img ~/Pictures/Wallpapers/w_6.jpg -t none --filter Nearest
$w7 = awww img ~/Pictures/Wallpapers/w_7.jpg -t none --filter Nearest
$w8 = awww img ~/Pictures/Wallpapers/w_8.jpg -t none --filter Nearest
$w9 = awww img ~/Pictures/Wallpapers/w_9.jpg -t none --filter Nearest
$w0 = awww img ~/Pictures/Wallpapers/w_0.jpg -t none --filter Nearest


################
### MONITORS ###
################
monitor=,2880x1920@120,auto, 1.6
# monitor=,2880x1920@120,auto, 1
# monitor=,2160x1440@40,auto, 1.25

# for fractional scaling issues
xwayland {
  force_zero_scaling = true
}

env = GDK_SCALE, 2
env = XCURSOR_SIZE,32


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = dolphin
$menu = wofi --show drun
$browser = zen-browser
$waybar = sh ~/.config/waybar/waybar.sh
$notesApp = obsidian


#################
### AUTOSTART ###
#################

exec-once = $waybar 
exec-once = hyprpaper
# exec-once = awww-daemon 
# exec-once = awww img "~/Pictures/Wallpapers/w_1.jpg" -t none

# kitty in my special spaec


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 10
    border_size = 4

    # gaps_in = 0 
    # gaps_out = 0
    # border_size = 3
    #
    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(63C0EFFF) rgba(00D5EfFF) 45deg
  
    # col.inactive_border = rgba(01345fFF)
    col.inactive_border = rgb(444444)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    # inactive_opacity = 0.90
    inactive_opacity = 1.0

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 15
        passes = 3

	      new_optimizations = true
	      ignore_opacity = true

	      noise = 0.05
	      contrast = 1
        # vibrancy = 0.1696
        vibrancy = 0.2
        # vibrancy = 0.1
	      vibrancy_darkness = 1
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    # animation = workspaces, noanim
    animation = workspaces, 1, 0.4, easeOutQuint, slide
    # animation = workspacesIn, 1, 1.21, almostLinear, fade
    # animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# layerrule=animation slide, hyprpaper

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]


# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    focus_on_activate = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0.2 # -1.0 - 1.0, 0 means no modification.

    accel_profile = flat

    touchpad {
        natural_scroll = true
    }

    # Keyboard key repeat
    repeat_delay = 200
    repeat_rate = 40
}

device {
    name = at-translated-set-2-keyboard
    kb_options = caps:swapescape
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
  name = razer-razer-viper-v3-hyperspeed-mouse
  accel_profile = linear
}

device {
    name = pixa3854:00-093a:0274-touchpad
    accel_profile = adaptive
}

###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# SICK, gestures now 
gesture = 4, right, dispatcher, exec, playerctl previous
gesture = 4, left, dispatcher, exec, playerctl next
gesture = 4, swipe, dispatcher, exec, playerctl play-pause

gesture = 3, swipe, scale: 2.0, resize
gesture = 3, swipe, mod: $mainMod, move

gesture = 3, pinchout, dispatcher, exec, pamixer -d 10
gesture = 3, pinchin, dispatcher, exec, pamixer -i 10


bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, W, exec, $browser
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, N, togglesplit, # dwindle
bind = $mainMod SHIFT, B, exec, sh ~/.config/waybar/waybar.sh 
bind = $mainMod SHIFT, Q, exec, ./duplicateKitty.sh
bind = $mainMod, F, fullscreen,
bind = $mainMod, O, exec, $notesApp
bind = $mainMod, M, exec, hyprlock

# maximze
bind = $mainMod, G, exec, ~/.config/hypr/maximize.sh

# Move focus with mainMod + vim motions
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# swap windows with vim motions
bind = $mainMod CTRL, h, movewindow, l
bind = $mainMod CTRL, j, movewindow, d
bind = $mainMod CTRL, k, movewindow, u
bind = $mainMod CTRL, l, movewindow, r

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 1, exec, $w1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 2, exec, $w2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 3, exec, $w3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 4, exec, $w4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 5, exec, $w5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 6, exec, $w6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 7, exec, $w7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 8, exec, $w8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 9, exec, $w9
bind = $mainMod, 0, workspace, 10
bind = $mainMod, 0, exec, $w0

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 1, exec, $w1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 2, exec, $w2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 3, exec, $w3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 4, exec, $w4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 5, exec, $w5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 6, exec, $w6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 7, exec, $w7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 8, exec, $w8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 9, exec, $w9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
bind = $mainMod SHIFT, 0, exec, $w0

# Resize active window
bind = $mainMod SHIFT, H, resizeactive, -40 0 # Shrink horizontally (left)
bind = $mainMod SHIFT, L, resizeactive, 40 0  # Grow horizontally (right)
bind = $mainMod SHIFT, K, resizeactive, 0 -40 # Shrink vertically (up)
bind = $mainMod SHIFT, J, resizeactive, 0 40  # Grow vertically (down)

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# screenshot 
bind = , Print, exec, if area=$(slurp); then grim -g "$area" - | tee >(wl-copy) > ~/Pictures/Screenshots/Screenshot-$(date +%F_%T).png && dunstify "Screenshot of the region taken" -t 1000; fi
bind = SHIFT, Print, exec, grim - | wl-copy && wl-paste > ~/Pictures/Screenshots/Screenshot-$(date +%F_%T).png | dunstify "Screenshot of whole screen taken" -t 1000 # screenshot of the whole screen
# hypricker
bind = $mainMod SHIFT, C, exec, hyprpicker | wl-copy

bind = SUPER SHIFT CTRL ALT, L, exec, xdg-open https://www.linkedin.com/feed/

##############################
### WINDOWS AND WORKSPACES ###
##############################

# Ignore maximize requests from apps. You'll probably like this.
windowrule {
  name = windowrule-1
  suppress_event = maximize
  match:class = .*
}


# Fix some dragging issues with XWayland
windowrule {
  name = windowrule-2
  no_focus = on
  match:class = ^$
  match:title = ^$
  match:xwayland = 1
  match:float = 1
  match:fullscreen = 0
  match:pin = 0
}


windowrule {
  name = windowrule-3
  opacity = 0.85 override 0.85 override
  match:class = ^(Spotify)$
}


# windowrulev2 = opacity 0.8 override, class:^(legcord)$

layerrule {
  name = layerrule-1
  blur = on
  match:namespace = waybar # Add blur to waybar
}

layerrule {
  name = layerrule-2
  blur_popups = on
  match:namespace = waybar # Blur waybar popups too!
}

layerrule {
  name = layerrule-3
  ignore_alpha = 0.2
  match:namespace = waybar # Make it so transparent parts are ignored
}


layerrule {
  name = layerrule-4
  blur = on
  match:namespace = wofi
}


layerrule {
  name = layerrule-5
  no_anim = on
  match:namespace = hyprpaper
}


windowrule {
  name = windowrule-4
  float = on
  pin = on
  match:class = ^(waybar)$
}

layerrule {
  name = layerrule-6
  animation = slide top
  match:namespace = waybar
}
